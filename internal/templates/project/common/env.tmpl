PORT=8080
DB_HOST=localhost
DB_PORT={{ if eq .DB "postgres" }}5432{{ else if eq .DB "mysql" }}3306{{ else }}0{{ end }}
DB_USER=user
DB_PASSWORD=password
DB_NAME={{ .ProjectName }}
DB_SSLMODE=disable

JWT_SECRET=your_super_secret_key
JWT_EXPIRY=72h
